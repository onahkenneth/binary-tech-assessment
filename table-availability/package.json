{
    "name": "table-service",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "start": "NODE_ENV=production bun run dist/server.js",
        "clean": "rm -rf dist && echo 'Done.'",
        "dev": "bun src/server.ts",
        "watch": "bun --watch src/server.ts",
        "build": "bun build src/server.ts --production --outdir=dist --target=bun"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5"
    }
}